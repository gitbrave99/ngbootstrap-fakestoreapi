import { TotalQuantityByCartPipe } from './total-quantity-by-cart.pipe';

describe('TotalQuantityByCartPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalQuantityByCartPipe();
    expect(pipe).toBeTruthy();
  });
});
