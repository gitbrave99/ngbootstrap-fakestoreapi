import { GetQuantityCartPipe } from './get-quantity-cart.pipe';

describe('GetQuantityCartPipe', () => {
  it('create an instance', () => {
    const pipe = new GetQuantityCartPipe();
    expect(pipe).toBeTruthy();
  });
});
